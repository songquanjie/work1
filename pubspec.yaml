name: echonote
description: 随身录音笔记 — 本地录音、列表、播放与删除。
publish_to: "none"
version: 1.3.0+4

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  record: ^6.1.1
  just_audio: ^0.10.4
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  provider: ^6.1.5
  path: ^1.9.1
  sqflite: ^2.4.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
